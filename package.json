{
  "name": "mern-template",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A template for MERN stack that has specific features for production applications",
  "scripts": {
    "backend": "nodemon --env-file backend/.env backend/app.js",
    "frontend": "yarn --cwd frontend dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\" ",
    "format": "prettier . --write"
  },
  "keywords": [],
  "author": "Zohaib Arsalan",
  "dependencies": {
    "concurrently": "^8.2.2",
    "c": "^3.1.0"
  },
  "devDependencies": {
    "prettier": "3.2.5"
  }
}
